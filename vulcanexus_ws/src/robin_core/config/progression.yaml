# Progression node configuration

progression_node:
  ros__parameters:
    # Publishing rate for progression tracking
    publish_rate: 100.0  # Hz

    # TF frames for TCP position
    base_frame: "base_link"
    tcp_frame: "wire_tip"
